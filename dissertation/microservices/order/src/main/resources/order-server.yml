# Spring properties
spring:
  application:
     name: order-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8999/eureka/

# HTTP Server
server:
  port: 8084   # HTTP (Tomcat) port
  